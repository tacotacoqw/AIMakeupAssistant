# 复制本文件为 .env 并填入实际值。
# .env 已在 .gitignore 中，不会进 git。

# 必填：从 https://aistudio.google.com/apikey 创建
GEMINI_API_KEY=your_api_key_here

# 可选：默认模型
GEMINI_MODEL=gemini-2.5-flash
GEMINI_LIVE_MODEL=gemini-2.0-flash-exp

# 可选：代理服务器监听端口
PORT=8888
